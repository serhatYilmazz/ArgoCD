apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: rollouts-demo-vsvc
spec:
  gateways:
  - rollouts-demo-gateway
  hosts:
  - '*'
  http:
  - name: primary
    route:
    - destination:
        host: bluegreen-demo-preview.argo-rollout-demo.svc.cluster.local
        port:
          number: 80
    - destination:
        host: bluegreen-demo.svc.cluster.local
        port:
          number: 80