apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: blue-green-demo-vsvc
spec:
  gateways:
  - rollouts-demo-gateway
  hosts:
  - '*'
  http:
  - name: primary
    route:
    - destination:
        host: bluegreen-demo-preview.argocd.svc.cluster.local
        port:
          number: 80
    - destination:
        host: bluegreen-demo.argocd.svc.cluster.local
        port:
          number: 80
      weight: 100